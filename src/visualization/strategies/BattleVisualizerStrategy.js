export class BattleVisualizerStrategy {
  initialize() {}
  
  async visualizeAction(action, attacker, defender, battleContext) {}
  
  displayBattleState(heroes, battleContext) {}
  
  handleBattleEvent(eventType, data) {}
}